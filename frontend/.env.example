# Backend origin for a SPLIT deploy (e.g. Cloudflare Pages frontend + Fly backend).
# Leave empty for local dev (Vite proxy) or a single-service deploy (FastAPI
# serves this bundle). On Cloudflare Pages, set this in the dashboard build env.
VITE_API_BASE=
