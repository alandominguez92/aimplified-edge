# Runtime deps for the hosted image. Excludes pybaseball (only needed offline to
# build the Statcast whiff table, which is bundled prebuilt in data/).
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pydantic==2.10.4
python-dotenv==1.0.1
scikit-learn==1.6.1
pandas==2.2.3
joblib==1.4.2
