.venv/
__pycache__/
*.pyc
.env
*.db
*.db-*
dev.log
jobs.log

# Compiled C++ engine (rebuild with engine/build.ps1)
engine/kelly_engine.exe
engine/*.o

# Trained models + Statcast table are COMMITTED so GitHub/CI builds bundle the
# ML model (small, ~4 MB). The large training CSV stays out (regenerable).
data/training.csv
